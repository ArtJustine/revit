/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove the exportPathMap configuration as it's not compatible with App Router
};

export default nextConfig;

